[package]
name = "mutual-follow-script"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mutual-follow"
path = "src/main.rs"

[dependencies]
zkat-host = { path = "../../../crates/host" }
mutual-follow-lib = { path = "../lib" }
sp1-sdk = { version = "6.0.2", features = ["blocking"] }
anyhow = "1"
clap = { version = "4", features = ["derive"] }

[build-dependencies]
sp1-build = "6.0.2"
